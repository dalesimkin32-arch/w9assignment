import M80NoPage from "@/components/M80NoPage";

export default function NotFound() {
  // Usually we just want to link back to a "safe" page on a not-found page, either home or a relevant page
  return <M80NoPage />;
}
